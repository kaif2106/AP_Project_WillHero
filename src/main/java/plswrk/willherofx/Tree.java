package plswrk.willherofx;

import javafx.scene.image.ImageView;

public class Tree extends GameElement{
    Tree(ImageView tree_image, double x, double y) {
        super(tree_image, null, x, y);
    }
}
